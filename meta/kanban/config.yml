implementation_skill: nextjs-developer   # Next.js 16 / React 19 TypeScript frontend

tdd: false            # prototype stage — no test suite established

review:
  enabled: true
  min_score: 8
  max_cycles: 3
  min_improvement: 0.5
  build_fix_attempts: 2
  test_command: "npm run build"   # next build; no test runner configured yet
  parallel: false

dispatcher:
  max_concurrent: 3

done:
  require_pr: false
  target_branch: ""   # auto-detect (main)

sizing:
  min_estimate: 0.5d
  max_estimate: 3d
